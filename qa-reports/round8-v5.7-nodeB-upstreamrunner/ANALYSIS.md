# persona-db API 實測分析報告 — `NODE-B` **v5.7**

**受測目標**：`NODE-B`（`[tailnet]`，node ID `NODE-B-NODEID`）
**服務版本**：**v5.7**（`/personadb/status` 確認，與 user 所述一致）
**執行時間**：2026-09-14 06:36 – 07:10 UTC（**34.5 分鐘**）
**執行結果**：**10 / 10 HTTP 200**（零錯誤）
**證據目錄**：`round8-v5.7-nodeB-upstreamrunner/`

> ## 本輪範圍（依 kstsai 指示）
> - ❌ **不與前版本比對**
> - ✅ **LLM Verify QA**：契約 + 斷言 + **API response 結果分析**
>
> 故本報告**不含**跨版本對照表、契約演進、top-k 交集等段落。

---

## 0. 目標與環境

| 項目 | 值 |
|:---|:---|
| 節點 | `NODE-B`，`[tailnet]`，node ID `NODE-B-NODEID` |
| **node ID 與 v5.2 / v5.4 / v5.6 三輪相同** | ⇒ **同一節點第 4 次就地升級** |
| 服務版本 | **v5.7** |
| Personas | 1069（1.72 MB）；Name diversity 172（16.1%）、max repeat 16× |
| Python / LLM | 3.11.15 / `deepseek-v4-flash` → `api.deepseek.com` |
| 連線 | `[public-ip]:41641`，**relay `hkg`** |

### ⚠️ 版本落差觀察（值得上游確認）

| 來源 | 版本 |
|:---|:---|
| 部署服務 `/personadb/status` | **v5.7** |
| repo `upDockerVerHermes/RELEASE-VERSION` | **v5.6** |

→ 兩者不一致。上游的 `#50` 斷言**正是檢查此事**（部署映像 vs `RELEASE-VERSION`），
但該項需 host 端 `sudo docker exec`，本 runner 為遠端 HTTP 執行 → **N/A，未驗**。
**這條落差本輪無法判定為「部署較新」或「repo 未更新」**，需在受測主機上跑上游原版確認。

---

## 1. 儀器

| 項目 | 值 |
|:---|:---|
| 上游腳本 | sha256 `5c1ca716…`（**227 行**，2026-09-14 取得）|
| 本輪 runner | sha256 `473d6e55…` |
| 請求參數 | **100% 未改**（與上游 `diff` 逐項比對 `--data-urlencode` 全同且順序一致）|

> 本輪上游與前一版（`9a29a8ef…`，223 行）的**唯一差異**是 `#42` 精度斷言由絕對門檻
> `len(have) >= 9` 改為**比例式** `len(s)>0 and len(have)/len(s) >= 0.9`（issue **#52**）。
> 本 runner 已同步該段，並以 `diff` 驗證**與新上游逐字相同**。

---

## 2. 執行摘要

| # | 案例 | HTTP | 耗時(s) | matched | returned | pool_exhausted | top score |
|---|------|------|---------|---------|----------|----------------|-----------|
| 0 | `/personadb/status` | 200 | 1.20 | — | — | — | — |
| 1 | 康是美（藥妝零售語意, #36） | 200 | 292.92 | 105 | 3 | false | 3.3180 |
| 2 | TESLA | 200 | 241.04 | 60 | 3 | false | 7.3971 |
| 3 | 時尚服裝設計師 | 200 | 85.29 | 81 | 10 | false | 4.5505 |
| 4 | 房貸 — 房仲業者 | 200 | 277.35 | 13 | 5 | false | 6.0815 |
| 5 | 房貸 — 銀行業者 | 200 | 214.76 | 8 | 5 | false | 5.5738 |
| 6 | 醫美診所（dim 20） | 200 | **588.95** | 12 | 10 | false | 5.0093 |
| 7 | 債務整合（dim 21） | 200 | 198.50 | 16 | 10 | false | 4.7287 |
| 8 | 小吃攤（**顧客語意**） | 200 | 130.81 | 75 | 10 | false | 4.7325 |
| 9 | **★業主本人（dim 22, #34）** | 200 | 42.54 | **61** | 10 | false | 1.7312 |

**Role QA diff check**：房仲 top **275** vs 銀行 top **328** → **✅ DIFFERENT**。
**契約不變式**：`returned == len(summary) == len(persona_ids)` **9/9 成立**；`score` 單調遞減 **9/9 成立**。

### 延遲

| | 值 |
|:---|:---|
| 10 案例總計 | 2072.2s（**34.5 分鐘**）|
| 平均 | **230.2s** |
| 範圍 | 42.5s（案例 9）～ **588.9s**（案例 6）|

> **案例 6 為明確離群**（588.9s ≈ 9.8 分鐘，是次慢者 292.9s 的 2 倍）。
> 其回應亦為本輪最大（8779 bytes）。**單次觀測，成因未定**（未取得伺服器端 log —
> 依 skill 的 `http=000` 歸因紀律，**不得**就此推論伺服器端問題）。

---

## 3. 上游斷言結果 —— **13/13 全數通過**

| 斷言 | 結果 |
|:---|:---|
| **#34 業主 query 必須套 `employment_status`** | ✅ `['雇主','自營作業者']` |
| **#34 顧客 query 不應套 `employment_status`** | ✅ `None`（語意正確） |
| #35 `dims_counted` 涵蓋 `applied_filters` | ✅ 4/4 案例無漏列 |
| #36 藥妝零售不應套 `aesthetic_procedure` | ✅ `None` |
| #36 醫美必須套 `aesthetic_procedure` | ✅ `['有']` |
| #37/#42 `no_op`+`overshoot` 欄位齊備 | ✅ 5/5 案例 |
| **#42 `debt_status` 精度** | ✅ **`10/10 (100%) pool_exhausted=False → ✅`** |
| #42 醫美硬篩選維持 | ✅ **`sex=['女'] aesthetic=['有']`** |
| #43 `returned` 與實際一致 | ✅ `returned=10 == len(summary)=10` |
| #44 `scoring_basis` 三欄位 | ✅ `weight_version=**v5.7**`、`score_scale=relative-within-version`、`score_schema=1` |
| #47 錯誤回應統一 `ErrorResponse` 形狀（負向測試） | ✅ `400` + `{"status":"error","error":{"code":"INVALID_OPMODE",…}}`，無 `detail` |
| #49 `opMode` 預設值合法 | ✅ `'僅篩選'` |
| #48 OpenAPI 型別化 schema | ✅ `BroadeningAttempt`/`ScoringBasis` properties 完整 |
| #50 部署版本一致性 | **N/A — 未執行**（需 host 權限）|

> **`#42` 的修復已在本輪驗證生效**：輸出格式改為 `10/10 (100%) pool_exhausted=False → ✅`
> —— 比起舊版的絕對門檻，新版同時給出**比例**與 **`pool_exhausted`**，可判讀性明顯提升。

---

## 4. 【核心】API response 結果分析

### 4.1 ✅ 語意正確性 —— 9/9

| 案例 | 關鍵 filter | 判定 |
|:---|:---|:---|
| 1 康是美 | `sex=['女']`、**無** `aesthetic_procedure`、`income=['<3萬','3-8萬']` | ✅ 藥妝零售不套醫美維度（#36）；target 明寫「女性、25-54 歲」 |
| 2 TESLA | `education=['大學','研究所以上']`、`housing_cost`、`income=['>8萬','3-8萬']`、`occupation=['科技','金融','醫療']` | ✅ 高單價耐久財 → 教育＋居住支出＋所得合理 |
| 3 時尚 | `clothing_spend=['3000~5000','>5000']` | ✅ 服飾消費力 |
| 4 房仲 | `marriage=['已婚']`、`family_size=['3','4']`、**`housing_burden=['低']`**、**`debt_status=['無']`** | ✅ 精準：找「居住負擔輕、無債務」的首購／換屋族 |
| 5 銀行 | **`housing_burden=['中','高']`**、**`debt_status=['有房貸','房貸+消費債']`**、`income=['>8萬']` | ✅ **與案例 4 方向完全相反** |
| 6 醫美 | **`sex=['女']`**、**`aesthetic_procedure=['有']`** | ✅ 雙重硬篩選 |
| 7 債務整合 | `housing_burden=['中','高']`、`debt_status=['有房貸','房貸+消費債']` | ✅ |
| 8 小吃攤（顧客） | **無** `employment_status` | ✅ #34 |
| 9 **業主本人** | **`employment_status=['雇主','自營作業者']`**、`age=['35-44','45-54','55-64']` | ✅ 語意最精準的一題 |

### 4.2 ★ 新增能力：`summary` 擴充至 **24 欄**，回應可「自我驗證」

本輪 `PersonaSummary` 欄位由 17 → **24**，新增 **`sex`、`marriage`、`education`、`hobby`、
`region`、`media_diet`、`politics`**（連同既有欄位共 24）。

**這關閉了第七輪 §4.5 指出的可觀測性缺口** —— 當時 `sex`/`marriage`/`education`/`hobby`
可出現在 `applied_filters`/`dims_counted` 卻**不在 `summary`**，導致消費端無法從回應驗證。
**✅ 現已可驗，且本輪實測全部通過：**

| 案例 | 驗證 | 結果 |
|:---|:---|:---|
| 1 康是美 | `sex=['女']` | **3/3 皆女** ✅ |
| 2 TESLA | `education=['大學','研究所以上']` | 全部符合 ✅ |
| 4 房仲 | `marriage=['已婚']` | **5/5 已婚** ✅ |
| 4 房仲 | `debt_status=['無']` | **5/5 無** ✅ |
| 5 銀行 | `debt_status=['有房貸','房貸+消費債']` | 4 + 1 ✅ |
| 6 醫美 | `sex=['女']` / `aesthetic_procedure=['有']` | **10/10、10/10** ✅ |
| 7 債務 | `debt_status=['有房貸','房貸+消費債']` | 7 + 3 ✅ |
| 9 業主 | `employment_status=['雇主','自營作業者']` | **10/10 雇主** ✅ |

**7 個新欄位在全部 66 筆回傳列皆有實值**（無 null／空）。`hobby` 為**多值陣列**
（例：`["烹飪","追劇","逛街購物"]`）。

→ **正面結論**：回應現在是**自證的** —— 可從 `summary` 直接確認每個 `applied_filter` 是否真的生效。

### 4.3 ✅ 計分宣告誠實（(b) 低報 = 0）

以「同 `dims_counted` 向量 ⇔ 同分」檢定，並**區分兩種情況**：
**(a) 同分但向量不同＝分數碰撞**（合法）、**(b) 同向量但分數不同＝低報**（真缺陷）。

| 案例 | 同分對 | (a) 碰撞 | **(b) 低報** |
|:---|:---:|:---:|:---:|
| 1 / 3 / 7 / 8 / 9 | 1 / 2 / 1 / 1 / 7 | 0 / 0 / 0 / 0 / **3** | **0 / 0 / 0 / 0 / 0** ✅ |
| 2 / 4 / 5 / 6 | 0 | 0 | 0（**空轉**：無同分對，不構成證據）|

**→ 5 個具檢定效力的案例中，(b) 低報全部為 0。**

### 4.4 ⚠️ Broadening：**60% 空轉**（9/15），且出現本系列首次 `overshoot`

| 案例 | 序列 | no_op | overshoot |
|:---|:---|:---:|:---:|
| **4 房仲** | **13→13 → 13→13 → 13→13** | **3** | 0 |
| 5 銀行 | 6→6 → 6→6 → 6→8 | 2 | 0 |
| 6 醫美 | 10→12 → 12→12 → 12→12 | 2 | 0 |
| 7 債務 | 6→6 → 6→6 → 6→16 | 2 | 0 |
| **2 TESLA** | 13→14 → 14→19 → **19→60** | 0 | **1** |
| 1 / 3 / 8 / 9 | 0 輪（首查即達標） | — | — |

**合計 9/15 = 60% 空轉**（`no_op`）。`no_op` 旗標與我人工判定**完全一致**。

#### ⚠️ 案例 4：3/3 迴圈**完全無效**，且第 1 輪的說法與事實不符

```
loop 1: 13→13  放寬職業別：在 occupation 中新增教育、醫療、其他…
loop 2: 13→13  放寬 housing_cost：由「<5千、5千~1.5萬」放寬為加入「1.5萬~3萬」
loop 3: 13→13  放寬 housing_cost 維度：…刪除後可保留其他核心判斷
```

- **loop 1 說要「新增教育、醫療、其他」，但該三值在 `applied_filters.occupation` 中本來就已存在**
  （`["科技","金融","製造","服務","公務","教育","醫療","其他"]`）⇒ **該輪是冗餘操作**
- loop 2 加入 `housing_cost` 值域 → 無效；loop 3 索性**整個移除** `housing_cost` → **仍然無效**
  ⇒ 證明 **`housing_cost` 根本不是約束條件**
- **真正的約束在別處**（`housing_burden=['低']` + `debt_status=['無']` + `marriage=['已婚']` + `family_size=['3','4']`）
  ⇒ **放寬機制挑錯了維度**，3 輪全部空轉

#### ⚠️ 案例 2（TESLA）：首次 `overshoot`，且移除的是**語意上關鍵**的維度

```
loop 3: 19→60  overshoot=true  移除「commute_mode」篩選維度（原僅限汽車）。
                               通勤方式並非本題核心條件…
```

- 放寬後樣本數由 19 → **60（3.2×）**，觸發 `overshoot`（上游定義：> 2×`TARGET_MIN`）
- 但**「以汽車通勤」對電動車目標客群是核心語意維度**，模型自己的 `reasoning` 也把它列為
  「消費維度」之一 → **自述意圖與放寬行為矛盾**
- 第七輪在 v5.6 上已觀察到同一模式（TESLA 把 `commute_mode` 放寬掉）；本輪**首次能量化為 overshoot**

### 4.5 ⚠️ 頭部集中：21 個 top-3 persona 中 4 個跨案例重複（19%）

| persona | 出現位置 |
|:---|:---|
| **TW-P-0234** | TESLA #3、**時尚 #1**、銀行 #3 |
| **TW-P-0473** | 銀行 #2、**醫美 #1**、債務 #2 |
| **TW-P-0328** | **銀行 #1、債務 #1** |
| TW-P-0275 | 時尚 #3、**房仲 #1** |

→ 少數 persona 在**不相關的產業** query 中反覆奪冠 ⇒ 下游若據此建名單，**跨題會重疊**。
**級別：證據**（同一輪 9 個獨立 query 的一致現象，可直接計數）。

### 4.6 分數尺度（僅記錄本輪數值，不做跨版本比較）

本輪 top score 範圍橫跨 **1.73（業主）～7.40（TESLA）**，而契約已明示
`score_scale = relative-within-version`（**不可跨版本比較**）。
→ 各案例分數**只在該次回應內部有相對意義**，不可與其他版本或其他案例直接比大小。

---

## 5. 驗證限制 / 未涵蓋範圍

1. **單次執行**：各案例的單筆數值不可視為穩定值（已知同輪同 query 就會變異）。
   本輪**未做重現性探針** —— 範圍為 QA 驗證，非變異研究。
2. **`#50` 未執行**（遠端無 host 權限）→ **N/A，未執行 ≠ 通過**。
   因此 §0 的「`RELEASE-VERSION` v5.6 vs 服務 v5.7」落差**本輪無法判定**。
3. **案例 6 的 588.9s 離群未歸因** —— 依 skill 紀律，未取得伺服器端證據前**不得**推論伺服器端問題。
4. **§4.3 的檢定在 4 個案例為空轉**（無同分對），不構成證據；有效者為 1/3/7/8/9。
5. **不與前版本比對**（依指示）。
6. 本報告節點以代號呈現；代號對照不在本 repo。

---

## 6. 結論

### ✅ 正面

1. **10/10 HTTP 200**，零錯誤；契約不變式 9/9、分數單調 9/9
2. **上游斷言 13/13 全數通過**（含 1 條負向測試 `#47`）
3. **語意正確性 9/9** —— 每個案例的 filter 都符合題意，且 `#34`／`#36` 的語意分流正確
4. **`summary` 擴充到 24 欄** ⇒ **回應變成自證的**：可直接從回傳列驗證每個 `applied_filter`
   （本輪 9 項可驗證查核**全部通過**）；第七輪指出的可觀測性缺口**已關閉**
5. **計分宣告誠實**：具檢定效力的 5 個案例中，低報 **(b) = 0**
6. **`#42` 的比例式修復已驗證生效**，輸出可讀性亦提升
7. 醫美案例**現在有 `sex=['女']`** 硬篩選（先前僅有 `aesthetic_procedure`）

### ⚠️ 需注意

1. **Broadening 空轉率 60%**（9/15）—— 較先前輪次明顯偏高
2. **案例 4 的 3 輪放寬全部無效**，且 loop 1 聲稱新增的職業值**本來就已存在**；
   loop 2/3 反覆調整 `housing_cost` 卻證明它並非約束 ⇒ **放寬機制挑錯維度**
3. **案例 2 首次觸發 `overshoot`**（19→60，3.2×），移除的是**語意上關鍵的 `commute_mode`**
4. **頭部集中 19%** —— 少數 persona 跨產業反覆出現
5. **案例 6 延遲離群 588.9s**（未歸因）
6. **`RELEASE-VERSION`(v5.6) 與服務版本(v5.7) 不一致** —— 需在 host 上驗 `#50`

### 🔧 建議

1. **Broadening 應先判斷「哪個維度是 binding constraint」再放寬** ——
   案例 4 的三輪空轉與案例 2 的過衝都是「挑錯維度」的兩種極端
2. **`commute_mode` 之類的語意核心維度應標記為不可放寬**（至少對汽車／通勤相關題型）
3. **補上 host 端的 `#50` 檢查**（部署流程一環），以解掉 `RELEASE-VERSION` 落差
4. 案例 6 的延遲離群建議在伺服器端查一次（WARNING log / `finish_reason`）

---

## 7. 相對於上游腳本的改動

| 項目 | 上游 | 本 runner |
|:---|:---|:---|
| Base URL | `http://localhost:8000` | `$BASE_URL` |
| 證據落盤 | `tee /tmp/*.json` | `raw/` + `headers/` + `meta/` |
| 斷言輸入 | `/tmp/*.json` | `$OUT/json/*.json` |
| `#47` 的 `B` | localhost | `$BASE_URL` |
| Role QA | `python3` 讀 `/tmp` | `jq` 讀 `json/`（等效） |
| **#50** | `sudo docker exec` | **N/A（不執行）** |

**請求參數、順序、斷言邏輯 100% 未變**：
`diff <(grep -o 'data-urlencode "[^"]*"' 上游) <(grep -o 'data-urlencode "[^"]*"' run-test.sh)` → 完全相同且順序一致；
`#42` 段亦與新上游**逐字相同**。
