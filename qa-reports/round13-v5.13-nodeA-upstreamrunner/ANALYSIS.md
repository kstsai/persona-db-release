# Persona DB API 實測分析報告 — v5.13（NODE-A / upstream runner）

> **範圍聲明**：本報告**只分析 v5.13 這個版本的回應**。依 `api-version-sweep` skill
> **v2.5.0**（2026-09-14 依 kstsai 指示取消跨版本比對），**本報告不含任何「與前版對照」的段落**。
> 若某條判讀是對我自己先前報告的更正，會標明「更正」，那不算比對。
>
> 執行時間（UTC）：2026-09-16 主套件 **00:45:48Z → 00:59:07Z**；探針隨後執行。
> 證據包：本目錄（`raw/` `headers/` `meta/` `json/` `extra/` `probe/`）。

---

## 0. 目標與環境

| 項目 | 值 |
|:--|:--|
| 節點 | `NODE-A`（私有 mesh VPN `[private-net]`；內部 hostname 見去識別化約定） |
| 部署型態 | Docker：`hermes` + `persona-db-api` 兩個容器 |
| repo `RELEASE-VERSION` | `v5.13` |
| 服務自報版本 | `/personadb/status` = `v5.13`；容器內 `/app/VERSION` = `v5.13` |
| **執行位置** | **節點本機**（`BASE_URL=http://localhost:8000`，與 upstream 原生環境相同） |
| 容器狀態 | `Up (healthy)`、`restarts=0`、started `2026-09-16T00:37:23Z` |
| 參照副本 | `persona-db-release` HEAD `5555600…`（== `origin/main`）；**節點上的 clone 為 `734677d`**（release 前一個 commit）；取得於 `2026-09-16T00:44Z` |

### 0.1 部署保真度 —— 「裝的到底是什麼」的機械證明

**版本字串不算證據**，所以逐層比對到 byte：

| 層 | 檢查 | 結果 |
|:--|:--|:--|
| 交付物 | repo 的 `persona-db-rel-v5.13.tar.gz` sha256 = `cbeb6040377548a40dd4ae2f…`；節點上同一檔 sha256 | **相同** ✅ |
| **tarball 內含 `.env`** | v5.13 release 明確排除（commit `523c687`）；實際檢查 | **0 個** ✅ |
| 落盤檔 | `/srv/persona-db-data/api/*.py` 對 tarball 內 `api/*.py` | **8/8 檔 sha256 相同** ✅ |
| 容器 | `/app` 是 `/srv/persona-db-data` 的 **bind mount**；容器內 `/app/api/*.py` sha256 亦同 | ✅ |
| 斷言 | `#50` 主機層：`部署版本 == RELEASE-VERSION (v5.13)` | ✅ |

→ **執行中的服務程式碼 == 發佈的 v5.13 產物**（byte 級）。

### 0.2 處置說明

1. **我沒有重新部署**：節點在我開始前已是 v5.13（容器起於 `00:37:23Z`，我的探測起於 `00:44Z`）。既然上面已證明部署 == 發佈產物，重新部署會是同義重複且會中途重啟服務。
2. **在節點本機執行**：主機層斷言可原生執行（`sudo` 免密碼），本輪因此**沒有斷言被標 N/A**。

> 依 `qa-reports/CORRECTIONS.md` **修正④**，此處已記錄參照副本的 commit sha 與取得時間；本輪部署版本與 repo `RELEASE-VERSION` **一致**，無版本落差。

---

## 1. 儀器

| 項目 | 值 |
|:--|:--|
| upstream | `upDockerVerHermes/test-persona-db-api.sh` |
| sha256 | `3d7b1de2ed802f7db2d2d9b3e9bfb54c92f01c89fe1b335b1b06a84ac0459774` |
| 行數 | **529**（v5.12 為 480；新增 `#66` 保護集上限/揭露、`#67` 主體覆蓋/依據，並修正 S/T） |
| 取得時間（UTC） | 2026-09-16T00:44Z |
| **runner sha256（執行前記錄）** | `928bd09cfdb7408be5486d59fd1bee6a6bf46c62db2819566b8c5020892d55ee`（519 行） |
| 原版留存 | `meta/original-upstream-3d7b1de2.sh`（逐字原檔） |

### 1.1 「不可改的沒被改」—— 機械證明

```
[1] 請求參數／順序  upstream=32 runner=32  ✅ 相同（含順序）
[2] 前段 L1-L14     ✅ 逐字相同
[3] 斷言段 L88-L529 ✅ 逐字相同（0 差異）
[4] 案例標籤 9/9    ✅ 逐案相同
```

作法：前段與斷言段以**原檔逐字切片**併入 runner（不經人手轉錄）；案例標籤**直接從 upstream 的 `echo` 行抽取原文**。
長跑等待一律用**完成標記檔**（`RUN_DONE`／`PROBE_DONE`），**不用** `pgrep -f`。

### 1.2 開跑前的檢查（skill v2.3.0/v2.4.0/v2.5.0 的檢查項）

**(a) 跨輪複製的工具是否有殘留舊路徑？** 逐一 grep 複製過來的工具：
只有 `make-runner.py` 出現 `qa-round9`，追查確認為**註解裡的反面教材**（非路徑預設值）；
`probe/run-probe.sh` 等其餘檔**無殘留**。runner 與 probe 的 `OUT` 皆為**自我定位**（`dirname $0`）。

**(b) harness 的執行時行為測試當場抓到我的疏失**：第一次跑 `meta/test-runner-harness.sh` 時
**3 項失敗** —— 因為我還沒建立 `probe/run-probe.sh`（該測試會從 probe 腳本抽出 `probe()` 來驗接線）。
補上後 **11/11 通過**。⇒ 這支測試確實在防「檢查全綠但行為是壞的」。

**(c) 本輪儀器的變動**：upstream 依第十二輪回饋**修好了 S/T 不變式**（見 §1.3）。

### 1.3 上游對第十二輪回饋的處置（供本輪驗證對象）

| 第十二輪的發現／建議 | v5.13 的處置 | 本輪驗證處 |
|:--|:--|:--|
| S/T 用了過嚴形式、且只掃 4/9 案例 | **改成 `widened ⊆ applied ∪ relaxed`，並擴到 9/9**；原始碼註解明載「採納外部不變式時必須同時讀該報告的更正與限制章節」 | §3、§4.7 |
| 「斷言 ✅」要附「掃了幾個案例」 | `#63` S' 與同質化矩陣的輸出**直接寫「掃描 9/9 案例」** | §3 |
| `subject` 覆蓋只有 2/9、且缺判定依據 | 新增 **`subject_basis`**；`subject` 覆蓋擴到 **8/9** | §4.1 |
| 保護集需揭露來源 | 新增 **`protected_dims_sources`**（五鍵）與 **`#66 A` 上限** | §4.6 (3)、§3.2 Y |
| （新機制）保護集飽和就停手 | 新增 **`protection_saturated`**（#66 C） | §4.6 (2)、§4.7 |

---

## 2. 執行摘要

| # | 案例 | HTTP | 耗時(s) | bytes | top_k | matched | returned | 輪數 | 停止原因 | `subject` | protected_dims |
|:--|:--|:--:|--:|--:|--:|--:|--:|--:|:--|:--|:--|
| 0 | `/personadb/status` | 200 | 0.6 | 792 | — | — | — | — | — | — | — |
| 1 | 康是美的目標客戶 | 200 | 77.3 | 4,097 | 3 | 27 | 3 | 1 | `target_reached` | customer | `['clothing_spend']` |
| 2 | TESLA的目標客戶 | 200 | 89.4 | 4,283 | 3 | 22 | 3 | 1 | `target_reached` | customer | `['commute_mode','family_income','income']` |
| 3 | 時尚服裝設計師的目標客戶 | 200 | 82.7 | 8,656 | 10 | **105** | 10 | 2 | `target_reached` | customer | `['clothing_spend']` |
| 4 | 房貸優惠 — 房仲業者 | 200 | 111.6 | 5,903 | 5 | 27 | 5 | 2 | `target_reached` | — | `['debt_status','family_income','housing_burden']` |
| 5 | 房貸優惠 — 銀行業者 | 200 | 99.4 | 6,755 | 5 | **9** | 5 | 3 | **`protected_veto`** | — | `['debt_status','family_income','housing_burden']` |
| 6 | 醫美診所的目標客戶 | 200 | 132.4 | 9,170 | 10 | 14 | 10 | 3 | `loop_limit` | customer | `['aesthetic_procedure']` |
| 7 | 債務整合的目標客戶 | 200 | 142.6 | 9,066 | 10 | 19 | 10 | 3 | `loop_limit` | customer | `['debt_status']` |
| 8 | **小吃攤老闆的目標客群（顧客語意）** | 200 | 42.4 | 7,876 | 10 | **291** | 10 | 0 | `target_reached` | **customer** | `[]` |
| 9 | 業主本人（B2B 受訪者） | 200 | 19.5 | 7,865 | 10 | 61 | 10 | 0 | `target_reached` | **owner** | `['employment_status']` |

- **10/10 HTTP 200**（另含 1 次刻意的負向測試 400）
- 9 個候選案例合計 **797.3s**，平均 **88.6s**，最快 **19.5s**（案例 9）、最慢 **142.6s**（案例 7）
- 全套 wall time **13 分 19 秒**（`00:45:48Z → 00:59:07Z`）
- 伺服器端窗口：11 個 `candidates` 請求、**0 個錯誤/例外**、`Protected dims` 8 筆、`Subject gate` 7 筆；
  另有 **2 個 WARNING**（1 個 overshoot、1 個 veto，皆為設計中的告警）—— 見 §4.6
- 只有 4/9 案例進入放寬迴圈；案例 5、6、7 的 `matched` 未達 `TARGET_MIN=20`（9、14、19）

---

## 3. 斷言結果

**合計：57 ✅ / 0 ⚠️ / 0 ❌ / 1 ℹ️**（完整輸出：`extra/assertions.txt`）

| 群組 | 結果 |
|:--|:--|
| `#34` must-use／must-not-use、`#35`、`#36`、`#37`、`#42` | ✅ |
| `#43`／`#44`／`#47`／`#48`／`#49`／`#53` | ✅ |
| `#56` 停止原因、`protected ∩ relaxed = ∅` | ✅ |
| **`#63` 不變式 S'** | ✅ **`widened ⊆ applied ∪ relaxed`，且輸出註明「掃描 9/9 案例」** |
| **`#63` 既放寬又被移除** | 資訊性：本輪無 |
| `#57` K/L/M veto 不變式 | ✅ **但本輪是空轉** —— 見 §3.1 |
| `#58`／`#59`／`#60`／`#61`／`#62` | ✅ |
| **`#65` 主體護欄** | ✅（主套件 8/9 案例有 `subject`；見 §4.1） |
| **`#65` 同質化矩陣** | ✅ **掃描 9/9 案例**，無 ≥8/10 配對 |
| **`#66 A` 保護集揭露** | ✅ 五鍵齊全；來源④上限 ≤ 3 成立 |
| **`#66 C` enum 含 `protection_saturated`** | ✅（靜態檢查） |
| **`#67 B` `subject_basis`／`protected_dims_sources`** | ✅ 全案例齊備 |
| `#50`／`#55` | ✅ 部署版本 == `RELEASE-VERSION (v5.13)` |

### 3.1 ⚠️ veto 相關斷言仍是「只掃 4/9 案例」，導致本輪出現兩件事

第十二輪我提出的「**每條斷言都要報它掃了幾個案例**」已被採納在 `#63` S' 與同質化矩陣上
（輸出直接寫「掃描 9/9 案例」）。但 **`#57` 的 veto 區塊沒有跟上**，它仍用
`_cases = {醫美, 債務, 業主, 小吃攤}`。本輪出現兩個後果：

**(a) 「本輪未觸發 veto」這句話是錯的**【證據】

斷言輸出：

```
#57 本輪未觸發 veto（保護維度皆未被嘗試移除）→ ℹ️
```

但**本輪確實發生了一次 veto**，在 `_cases` **之外**的案例 05（房貸-銀行）：

| 案例 | 逐輪 | 伺服器端 log |
|:--|:--|:--|
| 05 房貸(銀行) | `loop1: 6→8`、`loop2: 8→9`、**`loop3: veto=True vetoed=['family_income']`（rollback：`no_op=True`、`filters_changed=False`、`9→9`）** | `WARNING … Broadening loop 3: 受保護維度被變更 ['family_income'] → 還原該輪並停止（#57/#63）` |

`stop_reason` 也確實是 `'protected_veto'`。⇒ 該 ℹ️ 行讓讀者以為「這輪沒有 veto」，
**與事實相反**。

**(b) `#57` 的 K/L/M 三條 ✅ 是空轉**【空轉】

既然掃描集內沒有任何 veto 事件，K/L/M 三條的不合格清單必然是空的 ⇒ **✅ 不具檢定效力**。
我在**全部 9 個案例**上重算這三條，結論是：

| 不變式 | 全 9 案例重算 | 依據（案例 05） |
|:--|:--|:--|
| K：`vetoed_dims ⊆ protected_dims` | ✅ | `['family_income'] ⊆ ['debt_status','family_income','housing_burden']` |
| L：veto 必有非空 `vetoed_dims` | ✅ | `['family_income']` 非空 |
| M：veto 是 rollback | ✅ | `no_op=True`、`filters_changed=False`、`9→9` |

也就是**產品行為沒問題，是斷言的掃描範圍讓它看起來「沒事發生」**。
（第十二輪的同一類問題在 S'/T 已修；veto 區塊漏掉了。）

### 3.2 我額外加驗、upstream 沒有驗的不變式

`verify-extended.py`（A–AB）在**主套件 9 筆 + 探針 10 筆共 19 筆**上全部通過，其中新增四項：

| 檢查 | 內容 | 結果（涵蓋） |
|:--|:--|:--|
| **Y1–Y3** | `protected_dims_sources` 五鍵齊全；**來源聯集 ⊆ `protected_dims`**；**`protected_dims` 無未解釋項**（差集只能是 subject 禁用維度） | ✅ 19/19 |
| **Z** | **`stop_reason=='protection_saturated'` ⇒ 已套用維度全在保護集內**（#66 C 的定義不變式） | ✅ 2/2 個飽和案例 |
| **AA** | `subject_basis` 非空 ⟺ `subject` 非空（#67 B） | ✅ 19/19 |
| **AB** | `subject_basis` 的樣式與 `subject` 一致（customer→含「顧客語意樣式」…） | ✅ 所有非空案例 |
| S'/T、J、L、U、R、G/H 等 | 見 §4.7 | ✅ 19/19 |

---

## 4. API response 分析

> 級別：**【證據】**＝有 N≥3 取樣分布、機制性證明或可完整計數；**【觀察】**＝單次執行所見；**【空轉】**＝檢定本身無效。

### 4.1 語意正確性 —— 逐案例問「這個 filter 對得上題意嗎？」

| 檢查 | 案例 | 結果 | 級別 |
|:--|:--|:--|:--|
| **must-not-use 成立** | 08 小吃攤（顧客語意） | `subject='customer'`、**未套 `employment_status`**、名單為一般消費者 | **【證據】**（主套件 + 3 次重跑 4/4） |
| **must-use 成立** | 09 業主本人 | `subject='owner'`、套用 `employment_status=['雇主','自營作業者']` | 【觀察】 |
| **must-use 成立** | 06 醫美 | `aesthetic_procedure=['有']` 且 protected | 【觀察】 |
| **must-not-use 成立** | 01 藥妝零售 | 未套 `aesthetic_procedure` | 【觀察】 |
| **方向相反（正確）** | 04 房仲 vs 05 銀行 | 04 套 `debt_status=['無']`＋`housing_burden=['低']`；05 套 `debt_status=['有房貸','房貸+消費債']` → **兩者相反** | 【觀察】 |
| **語意關鍵維度受保護** | 02 TESLA | `commute_mode=['汽車']` 且 protected（`protect_only` 來源命中） | 【觀察】 |
| **主體判定覆蓋擴大** | 8/9 案例 | 01/02/03/06/07/08 = `customer`、09 = `owner`；只有 04/05（「房貸優惠方案」）為空字串 | 【觀察】 |

**`subject_basis` 讓主體判定可稽核**【證據】—— 這是第十二輪建議「回報判定依據」的落地：

| 案例 | `subject` | `subject_basis` |
|:--|:--|:--|
| 08 小吃攤 | `customer` | `命中「小吃攤老闆的目標客群」（顧客語意樣式）` |
| 01 康是美 | `customer` | `命中「康是美的目標客戶」（顧客語意樣式）` |
| 09 業主 | `owner` | `命中「企業主或工廠老闆本人」（業主語意樣式）` |

⇒ 現在任一條主體判定都能回溯到**題目原文的哪個片段**命中，不必再猜。

**`reasoning` 與實作的一致性**：機械抽取（自述「必須／需使用 X」vs `relaxed_dims`，**已處理否定詞**）
比對 9 個案例：**0 矛盾**。⚠️ 同一提醒：這個檢查抓不到主體誤判，那是 `#34`/`#65` 的職責。

**殘留（觀察）**：案例 06 的 `sex` 又被**放寬值集**為 `['女','男']`（reasoning 說「以女性為主」）——
與第十二輪同一型態；因為是值集放寬而非移除維度，仍**不進 `relaxed_dims`**（見 §6.3 建議）。

### 4.2 自證性 —— 用回傳列驗證 `applied_filters`

**19/19 項通過** ✅。**【證據】**

| 案例 | 檢查（節錄） |
|:--|:--|
| 02 | `commute_mode=['汽車'] → {'汽車': 3}` ✓；`housing_burden=['低','中'] → {'中': 3}` ✓ |
| 04 | `debt_status=['無'] → {'無': 5}` ✓ |
| 05 | `debt_status=['有房貸','房貸+消費債'] → {'有房貸': 4, '房貸+消費債': 1}` ✓ |
| 06 | `sex=['女','男'] → {'女': 10}` ✓（值集放寬見 §4.1） |
| 07 | `debt_status=[…] → {'有房貸': 9, '房貸+消費債': 1}` ✓ |
| 09 | `employment_status=['雇主','自營作業者'] → {'雇主': 10}` ✓ |

**可驗證性缺口：無**（`PersonaSummary` 曝露 24 欄；檢查 [D] 9/9 ✅）。

### 4.3 計分宣告誠實性 —— 必須區分兩種 mismatch

| 案例 | 列數 | 具檢定效力的配對 | **(a) 同分不同向量（碰撞）** | **(b) 同向量不同分（低報）** |
|:--|--:|--:|--:|--:|
| 08 小吃攤 | 10 | 5 | 0 | **0** |
| 07 債務 | 10 | 2 | 0 | 0 |
| 09 業主 | 10 | 2 | **2** | 0 |
| 06 醫美 | 10 | 1 | 1 | 0 |
| 03 時尚 | 10 | 1 | 0 | 0 |
| 04 房仲 | 5 | 0 | — | — |
| 01／02／05 | 3/3/5 | **0** | — | — |
| **合計** | — | **11** | **3** | **0** |

- **(b) 低報 = 0 ⇒ 沒有發現「有未列出的維度在影響計分」**。**【證據】**（就這 11 個配對）
- **(a) 碰撞 = 3**（2 個在業主題）—— **合法、非缺陷**。
- **必須主動宣告**：9 個案例有 **3 個是【空轉】**（配對數 0）；有效案例數 **6/9** 是本系列最好的一次。
- `score_scale = relative-within-version` ⇒ 分數只在同一次回應內有意義。

### 4.4 Broadening 行為

| 指標 | 值 | 級別 |
|:--|:--|:--|
| 總放寬輪數 | **15**（逐案 1+1+2+2+3+3+3） | **【證據】** |
| **空轉率（`no_op`）** | **1/15 = 7%**（案例 06） | **【證據】** |
| **`overshoot`** | **1/15 = 7%**（案例 03：`18→105` = **5.8×**，移除 `hobby`） | **【證據】** |
| 「每輪最多移除 1 維度」（#25 cap） | 成立 | **【證據】** |
| `no_op`／`overshoot` 旗標與數字一致 | **15/15 輪**（我的檢查 G/H） | **【證據】** |
| **值集放寬留痕（#63）** | **6 輪有痕跡** | **【證據】** |

**overshoot 詳情**（案例 03 時尚）：`loop2` 移除 `hobby`（`逛街購物`）使樣本 `18→105`（5.8×）。
`hobby` 不在保護集（`protected=['clothing_spend']`），所以放寬機制有權移除它；
但對「時尚服裝設計師的目標客戶」而言「逛街購物」是**語意上高度相關**的興趣維度 ——
這是「挑錯維度」型態的實例，`overshoot` 旗標正確標出。【觀察】（單次）

### 4.5 頭部集中 —— 同一批 persona 是否跨題重複

**7 / 54 個回傳過的 persona 出現在 ≥2 個案例**【證據】。
upstream 的**同質化矩陣（掃 9/9）顯示沒有任何 ≥8/10 的配對** ✅ ——
第十二輪那個「顧客名單≈業主名單」的指紋持續未出現。

### 4.6 其他觀察

**(1) `warnings` 從「從未被行使」變成「有內容且可稽核」**【證據】—— 第十二輪的【空轉】缺口已補

第十二輪 19 筆回應的 `warnings` 全空（那是該輪列出的限制之一）。**本輪 6 筆有內容**：
主套件案例 05、09 各 1 組，加上 4 個探針各 1 組。兩類告警都真的會產生：

| 告警類型 | 實例（案例 05） |
|:--|:--|
| **來源④上限**（#66 A） | 「模型宣告的保護維度超過上限 2 → 僅接受 `['debt_status','family_income']`」<br>「模型宣告的保護維度已達上限 2 → 忽略 `['housing_burden','income']`」 |
| **保護集覆蓋過半**（#66 A） | 「protected_dims 覆蓋 3/5 個已套用維度（`['debt_status','family_income','housing_burden']`）→ 放寬空間受限」 |

⇒ **上限機制不是裝飾**：案例 05 的模型一輪就宣告了 3 個保護維度，被上限裁到 2 個。

**(2) `protection_saturated`（#66 C）已驗證會動 —— 而且是我用定向探針逼出來的**【證據】

主套件 9 案例**都沒出現**（與出貨斷言的預期一致：「套件通常不會出現」）。我用
**刻意收窄的醫美查詢**（題目原文見 §4.7 方框）逼出它，另有 1 次在重跑中自然發生：

| 來源 | 觸發輪 | 保護集覆蓋 | 伺服器端 log |
|:--|:--|:--|:--|
| 探針 `t2`（設計的） | `loop 1` | `['aesthetic_procedure','age','marriage','sex']` = **4/4** | `Broadening loop 1: 保護集覆蓋全部已套用維度 … → stop=protection_saturated（#66 C）` |
| 探針 `r06_aesthetic_2`（自然） | `loop 3` | `['aesthetic_procedure']` = **1/1** | 同上 |

兩者都通過我加的定義不變式 **Z**（`applied_filters ⊆ protected_dims`）。
⇒ 「保護集飽和就停手、不靜默稀釋」這個設計**在真實請求上成立**。

**(3) `protected_dims_sources` 讓保護集可追溯**【證據】

例：案例 02（TESLA）
`{'domain': ['income'], 'analysis_declared': ['commute_mode','family_income'], 'model_declared': ['commute_mode','family_income','income'], 'protect_only': ['commute_mode']}`
⇒ 可以看出 `commute_mode` 是**三個來源同時**要求保護的（含 `protect_only` 這條 #61 的專用路徑）。
我的不變式 **Y** 在 19 筆上確認：**來源聯集 ⊆ `protected_dims`，且沒有任何「未解釋的保護項」**。

**(4) 兩則 WARNING 都是設計中的告警，不是錯誤**【證據】

伺服器端僅有兩則 WARNING：`overshoot 18 → 105`（§4.4）與 `受保護維度被變更 ['family_income'] → 還原該輪並停止`（§3.1）。
**0 個例外、0 次重試**。

**(5) `subject` 為空的兩題是「正確的空」**【證據】

04（房貸優惠 — 房仲業者）與 05（房貸優惠 — 銀行業者）的題目原文是「房貸優惠方案／房貸優惠方案」，
既無消費端名詞也無業主標記 ⇒ 依設計回 `''`（不設限）。**這不是缺陷**，但也意味著
**這兩題沒有主體護欄**（它們的語意分流靠 `role` 參數與模型自律）。

**(6) `#62` 空值清單：連續三輪未出現違規**；但部署碼的 `_normalize_filters()` **仍不留 log** ⇒
依然無法區分「沒產出」與「產出後被修復丟掉」（第十二輪已提，仍未落地）。

**(7) 錯誤回應**：本輪**沒有 5xx**。唯一的非 200 是斷言區塊刻意送出的負向測試
（`opMode=亂寫`）→ **400**，且 `#47` ✅ 確認它符合**宣告的 `ErrorResponse` 形狀**
（`error.code=INVALID_OPMODE`、**沒有** FastAPI 的 `detail` 包裝）。
**5xx 路徑本輪未驗證**（列於 §5）。

### 4.7 §6.8 重現性探針 與 §6.9 定向探針

> **探針題目原文（skill v2.4.0 規定：原文必須寫進報告，不可只寫「窄查詢」）**
>
> **`t1`** —— 刻意設計為「**超出 schema、必然不可滿足**」的查詢：
>
> ```
> questions=想找同時擁有遊艇與私人飛機的45歲單身女性企業主
> ```
>
> Persona DB 的 24 個欄位沒有「遊艇」也沒有「私人飛機」，所以它必然逼出 `matched=0` / `pool_exhausted=true`。
>
> **`t2`** —— 刻意設計為「**收窄到讓保護集飽和**」的查詢，用來逼出 v5.13 新增的 `protection_saturated`：
>
> ```
> questions=想找做過醫美療程的45歲單身女性
> ```
>
> 醫美題的保護集通常只有 `aesthetic_procedure`；把查詢收窄同時減少模型想套的維度，
> 就能讓「已套用維度 ⊆ 保護集」成立 → 進而觸發 #66 C 的停手分支。
>
> ⚠️ **兩者都是探針，不是業務案例，也不是產品缺陷主張**（「夜市小吃攤的老闆不會真的想找擁有遊艇的人」）。
> **限制**：人造查詢驗的是**分支機制**，不是真實流量 —— `matched=0` 在真實使用者身上可能由不同機制產生。
> `t1` 自第九輪起**重複作為回歸哨兵**；`t3`（TESLA top_k=20）與 `t4`（房貸-銀行 top_k=20）用主套件案例的參數放大 `top_k`。

#### §6.8 重現性

**案例 08（小吃攤，顧客語意）：4 次執行 —— 主體護欄與依據都穩定**【證據】

| 執行 | `subject` | `subject_basis` | `employment_status` | `matched` |
|:--|:--|:--|:--|--:|
| 主套件 | `customer` | `命中「小吃攤老闆的目標客群」（顧客語意樣式）` | 未套 | 291 |
| 探針 r1 | `customer` | 同上（逐字相同） | 未套 | 82 |
| 探針 r2 | `customer` | 同上 | 未套 | 212 |
| 探針 r3 | `customer` | 同上 | 未套 | 139 |

⇒ **`subject` 與 `subject_basis` 4/4 逐字相同**（判定完全可重現）；`employment_status` 4/4 未套。
`matched` 仍在 **82–291（3.5×）** 之間 ⇒ **護欄固定的是「詮釋」，不是「抽樣」**。

**案例 06（醫美）：4 次執行**【證據】

| 執行 | `matched` | `stop_reason` | `protected_dims` | `warnings` |
|:--|--:|:--|:--|:--|
| 主套件 | 14 | `loop_limit` | `[aesthetic_procedure]` | — |
| 探針 r1 | 16 | `loop_limit` | `[aesthetic_procedure]` | 覆蓋 1/1 |
| 探針 r2 | 16 | **`protection_saturated`** | `[aesthetic_procedure]` | 覆蓋 1/1 |
| 探針 r3 | 11 | `loop_limit` | `[aesthetic_procedure, age, education]` | 上限 3 → 裁到 3；覆蓋 3/6 |

⇒ 核心維度 `aesthetic_procedure` **4/4 受保護**；但 `matched` 11–16、`stop_reason` 與保護集大小每次不同
⇒ 單次細節只是【觀察】。

#### §6.9 定向探針 —— 逼出主套件未涵蓋的分支

| 探針 | 結果 | 判定 |
|:--|:--|:--|
| `t1`（不可滿足, top_k=10） | 200，`matched=0`、`status='too_strict'`、`pe=True`、**`protected_veto`（loop2 `vetoed_dims=['marriage']`）** | ✅ 達標 |
| **`t2`（收窄醫美）** | 200，`matched=1`、`pe=True`、**`stop_reason='protection_saturated'`（覆蓋 4/4）** | ✅ **達標 —— 逼出 #66 C 新分支** |
| `t3`（TESLA, top_k=20） | 200，`matched=23`、`returned=20`、`pe=False`、`commute_mode` 受保護未放寬 | ✅ 達標 |
| `t4`（房貸-銀行, top_k=20） | 200，`matched=12`、`pe=True`、`widened=['family_income','income']`、上限告警 1 則 | ✅ 達標 |

四個探針**全部產生 `warnings`**（第十二輪是 0 筆）⇒ 告警路徑已被充分行使。

---

## 5. 驗證限制 / 未涵蓋範圍

**必須明說「沒驗到什麼」—— N/A ≠ 通過。**

1. **`#57` 的 veto 區塊只掃 4/9 案例**（§3.1）：本輪唯一的 veto（案例 05）不在掃描集內 ⇒
   `#57 本輪未觸發 veto` 的 ℹ️ 與事實相反，且 K/L/M 三條 ✅ 是**空轉**。
   我在全 9 案例重算才確認 K/L/M 成立 —— **產品沒問題，是斷言範圍的問題**。
2. **`protection_saturated` 只觀測到 2 次**（1 次自然、1 次設計探針）；它的其他性質
   （例如與 `pool_exhausted` 的交互、或連續兩輪飽和的行為）未驗。
3. **`#66 A` 的「半數規則」只由單元測試驗證**（出貨斷言自述），本次回應只驗到「硬上限 3」。
4. **`#62` 修復仍不可稽核**（`_normalize_filters()` 不留 log）。
5. **`broadening_stop_reason` 只觀測到 4 種值**（`target_reached`、`loop_limit`、`protected_veto`、
   `protection_saturated`）；另 4 種（`llm_empty`、`llm_parse_error`、`llm_no_filters`、`no_filters`）未觸發；
   `""`／`budget_limit` 依處置說明 ⑤ 刻意保留。
6. **本輪沒有 5xx** ⇒ `503 FILTER_FAILED` 錯誤路徑未驗證；錯誤 schema 只涵蓋 400（由 `#47` 驗）。
7. **`subject` 有 2/9 為空**（案例 04／05）⇒ 這兩題**沒有主體護欄**。
8. **值集放寬仍不留痕於 `relaxed_dims`**（案例 06 的 `sex`）—— 只有 `widened_dims` 記錄，
   語意鬆動的「幅度」看不出來（只知道「有放寬」）。
9. **同一題的 `matched` 變異仍大**（案例 08：82↔291 = 3.5×；案例 03 因 overshoot 到 105）。
10. **只測了 `opMode=僅篩選`**；`篩選+模擬`、`模擬詢問` 未執行。
11. **未測**：並發／rate limit、認證、`/personadb/detail`、`top_k` 極端值。
12. **分數不可跨回應比較**（`relative-within-version`）。
13. **單一節點、單一部署、單一時段**。
14. **依 skill v2.5.0，本報告不做跨版本比對。**

---

## 6. 結論

### 6.1 這版好在哪（正面）

1. **第十二輪的兩條建議都落地且經本輪驗證有效**【證據】：
   ① **S' 的形式與涵蓋都修好了** —— 斷言輸出直接寫「`widened ⊆ applied ∪ relaxed`；**掃描 9/9 案例**」；
   ② **同質化矩陣也擴到 9/9**。
2. **主體護欄的覆蓋與可稽核性大幅提升**【證據】：`subject` 從第十二輪的 2/9 擴到 **8/9**；
   新增的 **`subject_basis`** 讓每次判定都能回溯到題目原文的命中片段
   （例：`命中「小吃攤老闆的目標客群」（顧客語意樣式）`），而且**4 次重跑逐字相同**。
3. **保護集來源可追溯**【證據】：`protected_dims_sources` 五鍵揭露每個維度由哪個來源保護
   （例：TESLA 的 `commute_mode` 同時被 `analysis_declared`／`model_declared`／`protect_only` 要求）；
   我的不變式 Y 在 19 筆上確認**沒有任何未解釋的保護項**。
4. **`#66 A` 的上限與告警真的會動**【證據】：`warnings` 由第十二輪的「19 筆全空」變成**本輪 6 筆有內容**，
   含「模型宣告的保護維度超過上限 → 僅接受…」與「protected_dims 覆蓋 3/5 個已套用維度 → 放寬空間受限」。
5. **`protection_saturated`（#66 C）確實會被觸發且行為正確**【證據】：
   我用定向探針 `t2` 在 `loop 1` 逼出它（覆蓋 4/4），另有 1 次在重跑中自然發生（`loop 3`，覆蓋 1/1）；
   兩者都滿足「已套用維度 ⊆ 保護集」的定義不變式。
6. **斷言全綠且不是空轉**：**57 ✅ / 0 ⚠️ / 0 ❌**，且本輪的統計檢定**有效案例數 6/9 是本系列最高**
   （計分低報 (b)=0），可驗證性缺口 0（19/19 自證通過）。
7. **放寬機制維持乾淨**：15 輪中 `no_op` 1 輪、旗標與數字 15/15 一致、值集放寬 6 輪有留痕；
   唯一的 `overshoot`（案例 03，5.8×）被旗標正確標出。
8. **語意方向正確**：房仲（`debt_status=['無']`）與銀行（`['有房貸',…]`）方向相反；
   藥妝未誤套醫美維度；業主題 must-use 成立。

### 6.2 這版要注意什麼

| # | 問題 | 級別 | 影響 |
|:--|:--|:--|:--|
| 1 | **`#57` veto 區塊只掃 4/9 案例** ⇒ 「本輪未觸發 veto」與事實相反（案例 05 有 veto），且 K/L/M 三條 ✅ 是**空轉** | **【空轉】＋【證據】** | 讀者會以為「這輪沒有 veto」；veto 不變式的保護力在本輪**沒有被真正檢驗**（我另在全 9 案例重算才確認成立） |
| 2 | **案例 03 出現 `overshoot`：移除 `hobby` 使樣本 18→105（5.8×）** | 【觀察】 | 對「時尚服裝設計師的目標客戶」而言「逛街購物」是高度相關的興趣維度，卻不在保護集內 → **挑錯維度**的實例 |
| 3 | **值集放寬仍不留「幅度」痕跡**（案例 06 `sex` 由 `['女']` 放寬為 `['女','男']`） | 【觀察】 | 語意鬆動看得到（`widened_dims`）但看不出鬆了多少，也不進 `relaxed_dims` |
| 4 | **`subject` 有 2/9 為空**（04／05 房貸題） | **【證據】** | 這兩題沒有主體護欄，語意分流靠 `role` 參數與模型自律 |
| 5 | **同一題 `matched` 變異仍大**（案例 08：82↔291 = 3.5×） | **【證據】** | 護欄固定詮釋、不固定抽樣；單次 sweep 數字不可作為品質結論 |
| 6 | `#62` 修復仍不留 log；`#66 A` 半數規則只有單元測試 | 源碼層證據 | 兩者都無法從 log 稽核 |

### 6.3 建議

1. **`#57` 的 veto 區塊比照 `#63` S' 改成掃描全部 9 案例，並在輸出報「掃描 N/9」**
   —— 這正是第十二輪已在 S'/T 與同質化矩陣落實的做法，veto 區塊是漏網的一塊。
   否則「未觸發 veto」會與事實相反，而 K/L/M 會一直是空轉 ✅。
   （本輪案例 05 就是現成的回歸素材：`vetoed=['family_income']`、rollback `9→9`。）
2. **`relaxed_dims`／`widened_dims` 之外，再揭露「放寬幅度」**（例如舊值集 → 新值集），
   讓「語意被鬆了多少」可稽核（案例 06 的 `sex: ['女'] → ['女','男']` 就是例子）。
3. **考慮把「語意高度相關的興趣／消費維度」納入保護候選**：案例 03 為衝樣本數移除 `hobby`
   造成 5.8× overshoot，而該維度對時尚題是相關的（`hobby=['逛街購物']` 正是模型自己套上的）。
4. **`_normalize_filters()` 觸發時寫一行 log**（第十二輪已提），讓 `#62` 可稽核。
5. **品管流程附取樣分布**：案例 08 的 82↔291 足以推翻任何單次判讀。

### 6.4 我對自己先前報告的更正

- **第十二輪的兩條建議已被採納並落實**（S' 形式＋涵蓋、斷言輸出報掃描案例數）—— 本輪逐項驗證有效。
- **第十二輪列出的「`warnings` 從未被行使」缺口，本輪已閉合**：6 筆回應有告警內容，
  且 `#66 A` 的上限真的裁掉了模型宣告的保護維度（案例 05）。
- **本輪我自己的檢查曾出錯，先修再下結論**：我新加的 `#66/#67` 檢查一開始把 `subj` 用在定義之前
  （`UnboundLocalError`），是**煙霧測試先抓到**才修正 —— 這正是「先驗檢查、再驗產品」的再一次實踐。
- **本輪發現一個新的空轉**：`#57` 的 K/L/M 因掃描範圍只有 4/9 而是空轉 ✅（§3.1）。
  我在報告中同時給出「全 9 案例重算」的結果，讓讀者知道**產品行為是對的、斷言範圍是錯的**。

---

---

## 7. 相對於原腳本的改動（儀器忠實度聲明）

除下列各項外，**請求參數、參數順序、斷言邏輯、案例標籤全部與 upstream 逐字相同**（§1.1 機械證明）：

| # | 改動 | 類別 | 說明 |
|:--|:--|:--|:--|
| 1 | 證據落盤：每個案例加 `-D headers/<case>.headers`、`-o raw/<case>.body`、`-w … > meta/<case>.w` | **落盤** | upstream 只有 `time curl … \| tee`，沒有 headers 與量測欄位 |
| 2 | 加 `-s` | 外觀 | 抑制進度表；不影響請求 |
| 3 | 案例 0 補一行標題 `=== 0. /personadb/status ===` | 外觀 | upstream 該案例無標題 |
| 4 | 案例 3 以相同方式落盤（`/tmp/fashion_closing.json`） | **落盤** | upstream v5.12 起自己也寫 `/tmp/`，故無特例分支 |
| 5 | 追加 `openapi.json` 快照 + `shasum` 一行 | 新增 | 供解讀回應與契約比對；不涉請求 |
| 6 | runner 環境變數化（`BASE_URL` / `OUT`）；**`OUT` 為自我定位** | 環境 | 本輪兩者皆為 upstream 原生值（`localhost:8000`、本輪目錄） |

**未改動**：`questions`/`top_k`/`role`/`opMode` 的參數值與順序（32 個 `data-urlencode` 逐一比對相同）、
所有斷言運算式、`/tmp/*.json` 的中介檔名。
